[package]
name = "llore_util"
version = "0.1.0"
edition = "2021"
license = "GPL-3.0-or-later"
description = "Utilidades para Llore Editor - paths, fs, extensions de Result/Option"

[lib]
path = "src/lib.rs"

[dependencies]
# Minimal dependencies
anyhow = "1.0"
dirs = "5.0"
parking_lot = "0.12"

[target.'cfg(unix)'.dependencies]
libc = "0.2"
